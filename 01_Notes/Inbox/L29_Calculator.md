# Lecture 29: Calculator

regard Scheme expressions as datas which can be represented as a class **Pairs** in Python.

A programming language:
- syntax
- semantics
To create a new language, it needs a specification and a canonical implementation(a compiler).

## Parse Expression

1. lexical analyzer: to partition the input string into **tokens**.
2. syntactic analyzer: to interpret token sequence as an expression **tree**.

## Evaluate Expression

1. eval function
2. apply function

## Interactive Interface

- REPL: read, evaluate, print and loop.