# tests

short-circuit.py:
- `and` and `or` always return the last thing they evaluate, whether they short circuit or not. Keep in mind that and and or don't always return booleans when using values **other than True and False**. 
- it's different in C, which always return false(0) or true(1).


